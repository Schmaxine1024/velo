<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      No ACCESS_BACKGROUND_LOCATION. A foreground service started while the app
      is visible keeps its location access for as long as it runs, which covers
      the whole ride. Asking for background location would trigger the Play
      Store's sensitive-permission review to buy nothing.
    -->

    <uses-feature android:name="android.hardware.location.gps" android:required="true" />

    <!--
      Package visibility. Since Android 11 an app cannot see another app's
      content provider unless it declares it here.

      PebbleKit.isWatchConnected() reads the Pebble app's state provider, so
      without this it returns null and the UI reports "watch not connected"
      forever, with no error and nothing in the log to explain it. The two
      authorities are the pre- and post-Basalt ones; the package names cover
      the original Pebble app and the Rebble build, which kept them.
    -->
    <queries>
        <package android:name="com.getpebble.android.basalt" />
        <package android:name="com.getpebble.android" />
        <provider android:authorities="com.getpebble.android.provider" />
        <provider android:authorities="com.getpebble.android.provider.basalt" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Velo">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HistoryActivity" android:exported="false" />
        <activity android:name=".RideDetailActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />

        <service
            android:name=".RideService"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>
</manifest>
